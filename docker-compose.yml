version: '3.8'

services:
  addon:
    build: .
    container_name: tg-stremio-addon
    ports:
      - "7860:7860"
    env_file:
      - .env
    volumes:
      - ./data:/app/data   # persistent SQLite cache + wizard config (survives restarts)
    restart: unless-stopped
